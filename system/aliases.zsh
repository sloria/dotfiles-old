# Pipe my public key to my clipboard.
alias pubkey="more ~/.ssh/id_rsa.pub | pbcopy | echo '=> Public key copied to pasteboard.'"

alias dotfiles="cd ~/.dotfiles && $EDITOR $ZSH"

alias cl="clear"
alias c="clear"

alias pg='ps -ef | grep'
